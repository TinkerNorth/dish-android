<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/spacing_lg"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tvAudioLabel"
        style="@style/TextAppearance.Dish.Label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Microphone" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAudioAction"
        style="@style/Widget.Dish.Button.Outlined"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xs"
        tools:text="Test microphone" />

    <ProgressBar
        android:id="@+id/barAudioLevel"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xs"
        android:max="100"
        android:visibility="gone"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/tvAudioStatus"
        style="@style/TextAppearance.Dish.Body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xs"
        tools:text="Listening…" />
</LinearLayout>
