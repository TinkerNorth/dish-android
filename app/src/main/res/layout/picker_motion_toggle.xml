<?xml version="1.0" encoding="utf-8"?>
<!-- Per-slot motion (gyro) on/off row inside the expanded controller card.
     Label column on the left (title + dynamic subtitle), SwitchCompat on the
     right. The adapter binds tvMotionSubtitle to whichever motion-capability
     state-derived string applies (no gyro / no host sink / on / off) and
     disables swMotion when the limit is hardware/host-side (not user-fixable
     by flipping the switch). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/picker_section_margin_top"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvMotionLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/controller_motion_toggle_label"
            android:textColor="@color/colorMuted"
            android:textSize="@dimen/text_label"
            android:fontFamily="monospace" />

        <TextView
            android:id="@+id/tvMotionSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorMuted"
            android:textSize="@dimen/text_label" />
    </LinearLayout>

    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/swMotion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical" />
</LinearLayout>
