<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/spacing_6xl"
    android:background="@color/colorSurface">

    <TextView
        android:id="@+id/tv_title"
        style="@style/TextAppearance.Dish.Title.Large"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/pairing_dialog_legacy_title" />

    <TextView
        android:id="@+id/tv_message"
        style="@style/TextAppearance.Dish.Body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_md"
        android:text="@string/pairing_dialog_legacy_message" />

    <!-- This layout is not inflated anywhere (see comment in strings.xml on
         pairing_dialog_legacy_*). Adopting Widget.Dish.EditText.Pin pulls
         in bg_pair_pin_field + the canonical PIN styling — harmless given
         the file is dead code retained for translation continuity. -->
    <EditText
        android:id="@+id/et_pin"
        style="@style/Widget.Dish.EditText.Pin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_3xl"
        android:hint="@string/pairing_dialog_legacy_pin_hint"
        android:maxLength="4" />

</LinearLayout>
