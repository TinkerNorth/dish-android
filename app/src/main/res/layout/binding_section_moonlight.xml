<?xml version="1.0" encoding="utf-8"?>
<!-- The Moonlight session a binding starts or joins: what the host will run, or
     why it cannot yet. Every state renders through this one slot, so the app
     rows, the note, and the action buttons are filled per state from code. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/ivSessionIcon"
            android:layout_width="@dimen/config_section_icon_size"
            android:layout_height="@dimen/config_section_icon_size"
            android:layout_marginEnd="@dimen/config_section_icon_gap"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_pc_monitor"
            app:tint="@color/colorMuted" />

        <TextView
            android:id="@+id/tvSessionEyebrow"
            style="@style/TextAppearance.Dish.Label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/ml_session_eyebrow"
            android:textAllCaps="true" />

        <ProgressBar
            android:id="@+id/sessionSpinner"
            android:layout_width="@dimen/config_callout_icon_size"
            android:layout_height="@dimen/config_callout_icon_size"
            android:indeterminate="true"
            android:indeterminateTint="@color/colorPrimary"
            android:visibility="gone"
            tools:visibility="visible" />
    </LinearLayout>

    <TextView
        android:id="@+id/tvSessionTitle"
        style="@style/TextAppearance.Dish.Title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/config_section_header_gap"
        tools:text="New session" />

    <TextView
        android:id="@+id/tvSessionBody"
        style="@style/TextAppearance.Dish.BodySmall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xs"
        android:textColor="@color/colorOnSurfaceVariant"
        tools:text="This is the first controller on the host, so it picks what it runs." />

    <LinearLayout
        android:id="@+id/sessionAppList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_md"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/tvSessionNote"
        style="@style/TextAppearance.Dish.Caption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_md"
        android:textColor="@color/colorMuted"
        android:visibility="gone"
        tools:text="Without a pick, Dish starts whatever the host lists first."
        tools:visibility="visible" />

    <LinearLayout
        android:id="@+id/sessionActions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xl"
        android:divider="@drawable/binding_gap_vertical"
        android:orientation="vertical"
        android:showDividers="middle"
        android:visibility="gone"
        tools:visibility="visible" />
</LinearLayout>
