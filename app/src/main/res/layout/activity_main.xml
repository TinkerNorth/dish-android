<?xml version="1.0" encoding="utf-8"?>
<!-- CoordinatorLayout root so Material Snackbar's swipe-to-dismiss gesture
     works (Snackbar.make walks up to find a CoordinatorLayout to host its
     dismiss behaviour). Drop-in for the previous FrameLayout — every child
     uses match_parent / wrap_content + optional layout_gravity which both
     containers honour identically. -->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    android:fitsSystemWindows="true">

    <!-- ═══ DASHBOARD ═══ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Fixed header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="@dimen/spacing_5xl"
            android:paddingTop="@dimen/spacing_6xl">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginBottom="@dimen/size_2">

                <TextView
                    style="@style/TextAppearance.Dish.Display.Large"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/brand_name" />

                <ImageButton
                    android:id="@+id/btnSettings"
                    android:layout_width="@dimen/icon_settings_button"
                    android:layout_height="@dimen/icon_settings_button"
                    android:src="@drawable/ic_settings"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/menu_settings"
                    android:padding="@dimen/spacing_md"
                    android:scaleType="fitCenter" />
            </LinearLayout>

            <!-- Connections summary card -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/cardConnections"
                style="@style/Widget.Dish.Card"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xl">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="@dimen/card_padding">

                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="vertical">

                        <TextView
                            style="@style/TextAppearance.Dish.Label.Accent"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/section_connections" />

                        <TextView
                            android:id="@+id/tvConnectionsSummary"
                            style="@style/TextAppearance.Dish.BodyLarge.Bold"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/dashboard_connections_empty"
                            android:layout_marginTop="@dimen/size_2" />
                    </LinearLayout>

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnManageConnections"
                        style="@style/Widget.Dish.Button.Outlined"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/action_manage"
                        android:textAllCaps="false" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <include
                android:id="@+id/sectionControllers"
                layout="@layout/section_header"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_2xl"
                android:layout_marginBottom="@dimen/spacing_md" />
        </LinearLayout>

        <!-- Scrollable slot list + telemetry -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvControllers"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingHorizontal="@dimen/spacing_5xl"
            android:paddingBottom="@dimen/spacing_3xl"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />
    </LinearLayout>

    <!-- Low-power overlays. The Snackbar-based feedback layer doesn't need
         a layout include — it's instantiated at runtime by
         DishNotifications via Snackbar.make() against this root view. -->
    <include layout="@layout/overlay_low_power" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
